{
  "name": "backs-zapcrm-monorepo",
  "version": "1.0.0",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "morgan": "^1.10.0",
    "jsonwebtoken": "^9.0.0"
  },
  "type": "module",
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build && cd .. && rm -rf .output && cp -r frontend/.output .",
    "start": "node server.js"
  }
}
